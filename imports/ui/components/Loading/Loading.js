/* eslint-disable */
import React from 'react';
import styled from 'styled-components';
if ( Meteor.isClient) import './Loading.scss'


const Loading = () => (
 
    <div className="loader">
      Loading...
    </div>
 
);

export default Loading;
